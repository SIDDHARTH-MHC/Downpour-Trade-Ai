# Research platform (Phase 1+) — optional; production API still uses SQLite by default.

# RESEARCH_DB_ENABLED=false
# RESEARCH_DATABASE_URL=postgresql+psycopg://downpour:downpour@localhost:5433/downpour_research
